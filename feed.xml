<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://bacnguyencong.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://bacnguyencong.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-22T08:45:18+00:00</updated><id>https://bacnguyencong.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Conditional flow matching</title><link href="https://bacnguyencong.github.io/blog/2026/conditional-flow-matching/" rel="alternate" type="text/html" title="Conditional flow matching"/><published>2026-04-15T15:12:00+00:00</published><updated>2026-04-15T15:12:00+00:00</updated><id>https://bacnguyencong.github.io/blog/2026/conditional-flow-matching</id><content type="html" xml:base="https://bacnguyencong.github.io/blog/2026/conditional-flow-matching/"><![CDATA[<blockquote> <p>This post explores the generalization of Conditional Flow Matching (CFM), a simulation-free objective for training generative models <a class="citation" href="#lipman2022flow">(Lipman et al., 2022)</a>. Notably, CFM removes the requirement for a Gaussian source distribution.</p> </blockquote> <h1 id="1-introduction">1. Introduction</h1> <p>Starting with a source distribution $q_0$, we define a time-dependent map $\phi: [0,1] \times \mathbb{R}^d \to \mathbb{R}^d$. This flow map is induced by a vector field $u:[0,1] \times \mathbb{R}^d\to \mathbb{R}^d$ through the following ordinary differential equation (ODE):</p> \[\begin{align*} \frac{d}{dt} \phi_t(x) &amp;= u_t(\phi_t(x)) \,, \\ \phi_0(x) &amp;= x \,. \end{align*}\] <p>A vector field $u_t$ is said to generate a probability density path $p_t$ if the corresponding flow map $\phi_t$ pushes forward the initial distribution such that</p> \[p_t = [\phi_t]_\#p_0 \,.\] <p>The flow map $\phi_t$ defines a pushforward $p_t$, representing the evolution of the density of samples $x \sim p_0$ as they are transported from initial state to time $t$. We can verify that the vector field $u_t$ generates this probability path $p_t$ by ensuring the continuity equation:</p> \[\frac{\partial p_t}{\partial t} + \text{div}(p_tu_t) = 0 \,,\] <p>where $\text{div}(.)$ indicates the divergence operator, which is defined as $\text{div}(F)(x) = \sum_i \frac{\partial F(x)_i}{ \partial x_i}$.</p> <p>The goal of Conditional Flow Matching (CFM) is to learn a neural vector field $u_\theta(x, t)$ such that its integration transforms a source distribution $q_0(x)$ into a target distribution $q_1(x)$. Once the velocity field is estimated, we can generate samples by following iits trajectories to transport data from $q_0$ to $q_1$. Next we describe how to learn this neural vector field.</p> <h1 id="2-flow-matching">2. Flow matching</h1> <p>While <a class="citation" href="#chen2018neural">(Chen et al., 2018)</a> proposed a direct method via differentiable ODE solvers, it is often computationally prohibitive. A more efficient alternative is to regress a vector field that induces a probability path between $p_0$ and $p_1$. However, there are infinite number of probability paths $p_t$ (equivalently infinite number of velocity fields $u(x, t)$)<d-footnote>Given a vector field and an initial distribution, the probability path is uniquely determined by pushing forward the density along the flow, but the reverse is not true. There exist multiple vector fields that generate the same probability path.</d-footnote> that transforms $q_0$ to $q_1$. Learning $u_\theta(x, t)$ requires us to explicitly define the target velocity field $u(x, t)$.</p> <p>Given a target velocity field, it leads to the flow matching objective</p> \[\mathcal{L}_\text{FM} = \mathbb{E}_{t, x\sim p_t(x)}[ \| u_t(x) - u_\theta(x, t) \|^2] \,.\] <h1 id="3-conditional-flow-matching">3. Conditional flow matching</h1> <p>To implement conditional flow matching, we only require access to the conditioning distribution, the conditional probability path and its corresponding conditional vector field. We will discuss several design choices.</p> <h2 id="31-vector-fields-generating-marginal-probability-paths">3.1. Vector fields generating marginal probability paths</h2> <p>We start by specifying a probability path $p_t$. Suppose that the marginal probability path $p_t(x)$ is a mixture of conditional probability paths with some conditioning variable $z$,</p> \[p_t(x) = \int p_t(x|z) q(z) dz \,,\] <p>where $q(z)$ is some distribution over the conditioning variable. It is important to satisfy the <strong>boundary condition</strong>, i.e., $\mathbb{E}_z [p_0(x \vert z)] = q_0 (x)$ and $\mathbb{E}_z[p_1(x\vert z)] = q_1(x)$. Therefore, we have the following results.</p> <blockquote class="block-tip"> <p>If the conditional probability path $p_t(x\vert z)$ is generated by the conditional vector field $u_t(x\vert z)$ from initial condition $p_0(x\vert z)$, then the vector field</p> \[u_t(x) = \int u_t(x|z) \frac{ p_t(x|z) q(z)}{p_t(x)} dz\] <p>generates the probability path $p_t(x)$.</p> </blockquote> <p>The CFM objective is defined as:</p> \[\mathcal{L}_\text{CFM} = \mathbb{E}_{t, z\sim q(z), x\sim p_t(x|z)}[ \| u_t(x|z) - u_\theta(x, t) \|^2]\] <p>Importantly, the gradients of FM and CFM w.r.t. the model parameters $\theta$ are identical. Thus, this allows us to regress against the simpler conditional vector field.</p> <p>When using Gaussian conditional probability paths of the form</p> \[\begin{align} p_t(x|z) &amp;= \mathcal{N}(x|\mu_t(z), \sigma_t(z)^2I) \,, \label{eq:cond} \end{align}\] <p>where $\mu: [0,1] \times \mathbb{R}^d \to \mathbb{R}^d$ denotes the time-dependent mean of the Gaussian distribution, while $\sigma: [0,1] \times \mathbb{R}^d \to \mathbb{R}_{&gt;0}$ describes a time-dependent scalar standard deviation. We have an important result</p> <p>The conditional vector field that generates conditional probability path in Equation \eqref{eq:cond} has the form of</p> <blockquote class="block-tip"> \[u_t(x|z) = \frac{\dot{\sigma}_t(z)}{\sigma_t(z)} (x - \mu_t(z)) + \dot{\mu}_t(z) \,.\] </blockquote> <h2 id="32-sources-of-conditional-probability-paths">3.2. Sources of conditional probability paths</h2> <p>There are several ways of choosing $q(z)$, $p_t(. \vert z)$, and $u_t(. \vert z)$. Table 1 shows the probability paths of existing methods <a class="citation" href="#tong2023improving">(Tong et al., 2023)</a>. In the following, we discuss some variants from prior work.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/cfm_method-480.webp 480w,/assets/img/cfm_method-800.webp 800w,/assets/img/cfm_method-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/cfm_method.png" class="img-fluid rounded w-150 mx-auto d-block" width="100%" height="auto" title="Probability path definitions for existing methods" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption"><b style="color: var(--global-theme-color)">Figure 1:</b> Probability path definitions for existing methods</figcaption> </figure> <h3 id="fm-from-gaussian">FM from Gaussian</h3> <p>By setting the condition $z=x_1$ and using a constant $\sigma &gt;0$, <a class="citation" href="#lipman2022flow">(Lipman et al., 2022)</a> define the Gaussian conditional path as:</p> \[p_t(x|z) = \mathcal{N}(x | tx_1, (t\sigma - t - 1)^2 I) \,.\] <p>This results in the conditional vector field:</p> \[u_t(x|z) = \frac{x_1 - (1-\sigma)x}{1 - (1-\sigma)t} \,.\] <p>In this framework, while each individual conditional path $p_t(x\vert z)$ follows an optimal transport (OT) trajectory from $p_0(x\vert z)$ to $p_1(x \vert z)$, the marginal path $p_t(x)$ is not in general an OT path from $p_0(x)$ to $p_1(x)$.</p> <h3 id="cfm-with-independent-coupling">CFM with independent coupling</h3> <p>Using an independent coupling $q(z) = q(x_0)q(x_1)$, we define the conditional probability path as a Gaussian centered on a linear interpolation:</p> \[p_t(x|z) = \mathcal{N}(x|tx_1 + (1-t)x_0, \sigma^2I) \,.\] <p>This leads to the conditional vector field:</p> \[u_t(x|z) = x_1 - x_0 \,.\] <p>Notably, the source $q_0(x)$ can be any arbitrary distribution, even with intractable densities. There is no requirement for $q_0(x)$ to be Gaussian. This independent CFM (I-CFM) is closely related to Rectified Flow <a class="citation" href="#liu2022rectified">(Liu, 2022)</a> and Stochastic Interpolant <a class="citation" href="#albergo2022building">(Albergo &amp; Vanden-Eijnden, 2022)</a>. Why I-CFM is capable of translating between any two arbitrary distributions, using it for unpaired translation typically results in content preservation issues. This is because I-CFM ignores the geometric structure of the data and fails to minimize the total displacement cost.</p> <h3 id="optimal-transport-cfm">Optimal transport CFM</h3> <p><a class="citation" href="#tong2023improving">(Tong et al., 2023)</a> demonstrated that the coupling $q(z)$ can be generalized so that $x_0$ and $x_1$ don’t have to be independent. The core CFM properties remain valid as long as $q(z)$ maintains the correct marginals $q(x_0)$ and $q(x_1)$. As a result, they propose $q(z)$ to be the 2-Wasserstein optimal transport plan $\pi$ such that $q(z) = \pi (x_0, x_1)$. Instead of sampling $x_0$ and $x_1$ independently, they are jointly sampled according to the optimal transport plan $\pi$. In practice, finding the global OT plan is computationally expensive for large datasets. We can instead compute a local OT coupling within each training minimatch.</p> <h3 id="schrödinger-bridge-cfm">Schrödinger bridge CFM</h3> <p>To improve the robustness of transport maps against noise and uncertainty in high-dimensional spaces, we can extend OT-CFM to Schrödinger bridge CFM (SB-CFM). We begin by defining the conditioning distribution as</p> \[q(z) = \pi_{2\sigma^2}(x_0, x_1) \,,\] <p>where $\pi_{2\sigma^2}$ represents the solution to the entropy-regularized OT problem using the cost $\Vert x_0 - x_1 \Vert$ and an entropy regularization $2\sigma^2$. The conditional probability path and vector field are defined as</p> \[\begin{align*} p_t (x | z) &amp;= \mathcal{N}(x | tx_1 + (1-t)x_0, t(1-t)\sigma^2 I) \\ u_t(x| z) &amp;= \frac{1-2t}{2t(1-t)} (x - (tx_1 + (1-t)x_0)) + (x_1 - x_0) \,. \end{align*}\] <p>By construction, the marginal vector field $u_t$ generates the same probability path as the solution fo the SB problem.</p> <h1 id="conclusion">Conclusion</h1> <p>In this post, we have explored how Conditional Flow Matching (CFM) provides a simulation-free framework for training generative models. CFM bypasses the high computational costs of traditional neural ODEs and offers flexibility than diffusion models, moving beyond the requirement for Gaussian source distributions.</p> <h1 id="references">References</h1> <ol class="bibliography"><li><div class="row"> <div id="lipman2022flow" class="col-sm-12"> <span id="lipman2022flow">Lipman, Y., Chen, R. T. Q., Ben-Hamu, H., Nickel, M., &amp; Le, M. (2022). Flow matching for generative modeling. <i>ArXiv Preprint ArXiv:2210.02747</i>.</span> </div> </div> </li> <li><div class="row"> <div id="chen2018neural" class="col-sm-12"> <span id="chen2018neural">Chen, R. T. Q., Rubanova, Y., Bettencourt, J., &amp; Duvenaud, D. K. (2018). Neural ordinary differential equations. <i>Advances in Neural Information Processing Systems</i>, <i>31</i>.</span> </div> </div> </li> <li><div class="row"> <div id="tong2023improving" class="col-sm-12"> <span id="tong2023improving">Tong, A., Fatras, K., Malkin, N., Huguet, G., Zhang, Y., Rector-Brooks, J., Wolf, G., &amp; Bengio, Y. (2023). Improving and generalizing flow-based generative models with minibatch optimal transport. <i>ArXiv Preprint ArXiv:2302.00482</i>.</span> </div> </div> </li> <li><div class="row"> <div id="liu2022rectified" class="col-sm-12"> <span id="liu2022rectified">Liu, Q. (2022). Rectified flow: A marginal preserving approach to optimal transport. <i>ArXiv Preprint ArXiv:2209.14577</i>.</span> </div> </div> </li> <li><div class="row"> <div id="albergo2022building" class="col-sm-12"> <span id="albergo2022building">Albergo, M. S., &amp; Vanden-Eijnden, E. (2022). Building normalizing flows with stochastic interpolants. <i>ArXiv Preprint ArXiv:2209.15571</i>.</span> </div> </div> </li></ol>]]></content><author><name></name></author><category term="deep-generative-modeling"/><category term="conditional-flow-matching"/><summary type="html"><![CDATA[This post explores the generalization of Conditional Flow Matching (CFM), a simulation-free objective for training generative models (Lipman et al., 2022). Notably, CFM removes the requirement for a Gaussian source distribution.]]></summary></entry><entry><title type="html">Drifting models</title><link href="https://bacnguyencong.github.io/blog/2026/drifting-models/" rel="alternate" type="text/html" title="Drifting models"/><published>2026-03-06T15:12:00+00:00</published><updated>2026-03-06T15:12:00+00:00</updated><id>https://bacnguyencong.github.io/blog/2026/drifting-models</id><content type="html" xml:base="https://bacnguyencong.github.io/blog/2026/drifting-models/"><![CDATA[<blockquote> <p>This post explores some key insights behind drifting models <a class="citation" href="#deng2026generative">(Deng et al., 2026)</a>, a framework that enables high-fidelity one-step generation. By shifting the iterative process to the training phase, this approach has achieved state-of-the-art results on ImageNet.</p> </blockquote> <h1 id="1-training-objective">1. Training objective</h1> <p>Let $f_\theta\colon \mathbb{R}^C \to \mathbb{R}^D$ be a function (parameterized by $\theta$) that maps a noise $\epsilon \sim p_\epsilon$ in $\mathbb{R}^C$ to an output $x=f_\theta(\epsilon) \sim q_\theta$ in $\mathbb{R}^D$. The distribution of outputs $q_\theta$ is also referred to as the <em>pushforward</em> distribution under $f_\theta$, i.e.,</p> \[q_\theta=(f_\theta)\#p_\epsilon \,.\] <p>In other words, $f_\theta$ transforms a base distribution $p_\epsilon$ into the pushforward distribution $q_\theta$. Our objective is to find $f_\theta$ such that $q_\theta \approx p$ under some statistical divergence, where $p$ denotes the data distribution.</p> <p>Let $\Delta_{p,q_\theta}\colon \mathbb{R}^D \to \mathbb{R}^D$ denote a <strong>drifting field</strong> that describes how the output sample $x\sim q_\theta$ should move to match the data distribution $p$. It can be seen as a guidance force for the model output during training. It measures the force required to push samples from $q_\theta$ to $p$. The drifting field must satisfy an equilibrium condition, i.e.,</p> \[\begin{align} \Delta_{p, q_\theta}(x) = 0 \, \text{ if and only if } \, q_\theta = p\,. \label{eq:equilibrium} \end{align}\] <p>The equilibrium condition is critical for ensuring that the training process converges accurately and stays stable. It yields the following two properties:</p> <ul> <li><strong>Correctness</strong> ($\Delta_{p, q_\theta}=0 \rightarrow p=q_\theta$): This direction guarantees that if the drifting field vanishes, the generator has successfully matched the pushforward distribution $q_\theta$ to the target data distribution $p$.</li> <li><strong>Stability</strong> ($p=q_\theta \rightarrow \Delta_{p, q_\theta}=0$): This direction ensures that once the distributions match, the drifting field becomes zero, making the evolution of distribution stops changing and remains at the equilibrium.</li> </ul> <p>Once the drifting field $\Delta_{p,q_\theta}$ is defined, the primary objective is to minimize its norm. Essentially, the training process aims to make the drift vanishes, indicating the model has reached its target distribution:</p> \[\begin{align} \mathcal{L}(\theta) = \mathbb{E}_{\epsilon \sim p_\epsilon}\left[ \| \Delta_{p,q_\theta}(f_\theta(\epsilon)) \|^2 \right] \label{eq:vanilla} \,. \end{align}\] <p>It is important to note that the drifting field is not a learnable neural network; rather, it is a fixed mathematical operator that determines the relationship between $p$ and $q_\theta$ at a given point $x$. Directly minimizing the objective in Equation \eqref{eq:vanilla} is challenging because it requires back-propagating gradients through the drifting field $\Delta_{p,q_\theta}$. As this field depends on the current generated distribution $q_\theta$, computing gradients becomes complex.</p> <p>To address the challenge of back-propagating through the distribution-dependent drifting field, <a class="citation" href="#deng2026generative">(Deng et al., 2026)</a> proposed an indirect optimization approach using the following surrogate objective:</p> \[\begin{align} \mathcal{L}_\mathrm{drift}(\theta) = \mathbb{E}_{\epsilon \sim p_\epsilon}\left[ \| f_\theta(\epsilon) - \mathrm{stopgrad} \big(f_\theta(\epsilon) - \Delta_{p,q_\theta}(f_\theta(\epsilon)) \big) \|^2 \right] \label{eq:surrogate} \end{align}\] <p>While this loss function is mathematically equivalent to the original objective, the integration of a stop-gradient operator eliminates the need to differentiate through the complex drifting field itself.</p> <p>Once the objective function is defined, the logical next question becomes: <strong>how exactly is this drifting field constructed?</strong> We will explore the specific mechanisms for calculating it in the following section.</p> <h1 id="2-drifting-fields">2. Drifting fields</h1> <p>In this section, we describe a specific method to construct the drifting field. Note that this is not the only feasible approach. Any formulation of the drifting field is considered valid as long as the equilibrium condition \eqref{eq:equilibrium} is satisfied.</p> <p>To define the drifting field component for a given distribution $\pi$ (which can be either the data $p$ or generated distribution $q_\theta$), we use the mean-shift vector field formulation</p> \[V_{\pi,k}(x) = \frac{\int \pi(y) k(x, y)y dy}{ \int \pi(y) k(x, y) dy} - x\,,\] <p>where $k(.,.) \ge 0$ is a similarity kernel, denoting how much each sample $y$ influences the drift at $x$. In a discrete setting with a mini-batch of samples ${y_j}_{j=1}^N$ drawn from $\pi$, we can approximate it as</p> \[V_{\pi,k}(x) \approx \frac{\sum_{j=1}^N k(x, y_j)y_j}{ \sum_{j=1}^N k(x, y_j)} - x\,.\] <p>The drifting field $\Delta_{p,q_\theta}$ is then defined as</p> \[\begin{align} \Delta_{p,q_\theta}(x) = V_{p,k}(x) - V_{q_\theta,k}(x) \,. \end{align}\] <p>The drifting field is driven by two opposing terms, <span style="color: red;">attraction</span> ($V_{p,k}(x)$) and <span style="color: blue;">repulsion</span> ($V_{q_\theta,k}(x)$). Intuitively, this mechanism pulls samples toward the real data distribution while pushing them away from the generated distribution.</p> <p>We employ a Laplace kernel $k_\tau(.,.)$ scaled by a temperature parameter $\tau$:</p> \[k_{\tau}(x, y) = \mathrm{exp}\left(-\frac{1}{\tau}\|x - y\|\right)\,.\] <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/drifting_models/drifting_field-480.webp 480w,/assets/img/drifting_models/drifting_field-800.webp 800w,/assets/img/drifting_models/drifting_field-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/drifting_models/drifting_field.png" class="img-fluid rounded w-75 mx-auto d-block" width="100%" height="auto" title="Drifting field" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption"><b style="color: var(--global-theme-color)">Figure 1:</b> Generated samples drift toward data</figcaption> </figure> <p id="fig-drifting-field">As a visual illustration, <a href="#fig-drifting-field">Figure 1</a> demonstrates how the drifting vectors effectively guide the generated points toward the real data manifold.</p> <h1 id="3-connection-to-previous-works">3. Connection to previous works</h1> <p>This section explores how drifting models relate to existing generative paradigms, highlighting how they bridge the gap between iterative refinement and single-step generation.</p> <h2 id="31-score-based-generative-models-sgm">3.1. Score-Based Generative Models (SGM)</h2> <p>A recent study by <a class="citation" href="#lai2026unified">(Lai et al., 2026)</a> established that drifting can be interpreted as a score-based method acting on kernel-smoothed distributions. Specifically, by defining a smoothing field $\pi_k(x)$ via a kernel $k$</p> \[\pi_k(x) = \mathbb{E}_{y\sim \pi}[k(x, y)] = \int k(x, y) \pi(y) dy \,,\] <p>we can derive the associated score function $s_{\pi,k} = \nabla_x \log \pi_{k}(x)$. When employing a Gaussian kernel $k_\tau=\mathrm{exp}\left(-\frac{1}{2\tau^2}\Vert x - y \Vert^2\right)$, the objective function \eqref{eq:surrogate} simplifies to</p> \[\mathcal{L}_\mathrm{drift}(\theta) \propto \mathbb{E}_{x\sim q_\theta}\left[ \| s_{p, k_\tau} - s_{q_\theta, k_\tau} \|^2\right] \,.\] <p>This result demonstrates that minimizing the drifting objective is mathematically equivalent to minimizing the reverse Fisher divergence between the kernel-smoothed target and model distributions. Note that the expectation in this formulation is taken with respect to samples drawn from $q_\theta$.</p> <h2 id="32-distribution-matching-distillation-dmd">3.2. Distribution Matching Distillation (DMD)</h2> <p>DMD <a class="citation" href="#yin2024one">(Yin et al., 2024)</a> assumes a high-quality, pretrained diffusion model $p$ is available to act as a “teacher”. Much like Generative Moment Matching Networks (GMMNs) <a class="citation" href="#li2015generative">(Li et al., 2015)</a>, the core objective of DMD is to align the distribution produced by a student network ($q_\theta$) with that of the teacher ($p$). Because the score function is poorly defined in low-density regions, DMD perturbs the generated sample $f_\theta(\epsilon)$ using a diffusion process</p> \[\hat{x}_t = \alpha_t f_\theta(z) + \sigma_t \epsilon\] <p>This perturbation yields a set of marginal distributions $q_{\theta,t}$ and $p_t$ for any given timestep $t$. DMD minimizes a weighted sum of KL divergences between these marginals:</p> \[\mathcal{L}_\mathrm{DMD}(\theta) = \mathbb{E}_{t}\left[ \omega(t) \mathcal{D}_\mathrm{KL}(q_{\theta,t} \| p_t)\right] \,.\] <p>Taking the gradient of this loss with respect to the generator parameters, we get a clear “push-pull” signal based on the difference between the student and teacher scores</p> \[\nabla_\theta \mathcal{L}_\mathrm{DMD}(\theta) = \mathbb{E}_{t,z,\epsilon}\left[ \omega(t) \left(\nabla_x \log q_{\theta}(\hat{x}_t, t) - \nabla_x \log p(\hat{x}_t, t) \right)\frac{d f_\theta}{d\theta}\right] \,.\] <p>In DMD, the teacher scores ($s_{p_t}$) are directly provided by the frozen pre-trained diffusion model, while the student scores ($s_{q_{\theta,t}}$) are estimated by an auxiliary model that is trained alongside the generator.</p> <p>Taking the gradient of $\mathcal{L}_\mathrm{drift}(\theta)$ in Equation \eqref{eq:surrogate} with respect to the parameters $\theta$, we obtain</p> \[\nabla_x \mathcal{L}_\mathrm{drift}(\theta) = \mathbb{E}_{\epsilon}\left[ \Delta_{p,q_\theta}(f(\epsilon)) \frac{df_\theta}{d\theta} \right] \,.\] <p>While DMD relies on a “push-pull” signal derived from the difference between two learned score functions, drifting models simplify this process by using the drifting field itself as the guiding signal. This field acts as a direct vector to distill the data distribution into the generator without the need for auxiliary score-based models.</p> <h2 id="33-generative-adversarial-networks-gans">3.3. Generative Adversarial Networks (GANs)</h2> <p>Coulomb GANs <a class="citation" href="#unterthiner2017coulomb">(Unterthiner et al., 2017)</a> consider the GAN optimization problem as learning a potential field, where generated examples are pulled to the training data while being pushed away from one another. The generator updates its parameters to move samples along a vector field defined by the gradient of a potential field, which is learned by the discriminator. The potential $\Phi$ at any point $x$ is:</p> \[\Phi_{p,q_\theta}(x) = \int q_\theta(x) k(x, y) dy - \int p(x) k(x, y) dy = \mathbb{E}_{y\sim q_\theta}[k(x, y)] - \mathbb{E}_{y\sim p}[k(x, y)] \,.\] <p>This formulation looks very simiar to the mean-shift vector field used in the drifting models, with one key difference: it lacks the normalization factor. In Coulomb GANs, the discrimiator ($D(.)$) task is to approximate this potential field $\Phi$, while the generator aims to shift its output toward regions where $D(x)$ is minimized. In contrast, drifting models completely eliminate the need for an adversarial training loop.</p> <h1 id="4-practical-implementation">4. Practical implementation</h1> <h2 id="41-dependence-on-tau">4.1. Dependence on $\tau$</h2> <p>The theoretical solution depends on the equilibrium condition. As long as the kernel $k_\tau(.,.)$ is positive and characteristic, the only distribution that makes the drift vanish is the data distribution $p$. The temperature $\tau$ does not change what the model is trying to learn. However, the temperature does changes the loss landscape.</p> <ul> <li>Low temperature (small $\tau$): The drifting field becomes sharp and local as only data points close to the generative sample have strong influence. This helps the model to capture fine-grained and local structure of the data distribution.</li> <li>High temperature (large $\tau$): The drifting field becomes smooth and global as data points that are far away also have influence. This helps the model to capture coarse and global structure of the data distribution.</li> </ul> <p>In the example below we visualize the evolution of $q$ (<span style="color: blue;">blue</span> points) toward data distribution $p$ (<span style="color: red;">red</span> points). Clearly, the training dynamics are affected by temperatures. To ensure the model is robust against specific kernel settings, the authors employ multiple temperatures simultaneously, by summing the contributions from each temperature scale.</p> <div class="row justify-content-sm-center"> <div class="col-sm-4 mt-3 mt-md-0"> <figure> <video src="/assets/video/drifting_models/t_0.1.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> <div class="col-sm-4 mt-3 mt-md-0"> <figure> <video src="/assets/video/drifting_models/t_2.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> <div class="col-sm-4 mt-3 mt-md-0"> <figure> <video src="/assets/video/drifting_models/t_0.1.2.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <h2 id="42-feature-encoder">4.2. Feature encoder</h2> <p>The objective function in Equation \eqref{eq:surrogate} is originally defined in the raw data space, which means the drifting field must be estimated in an extremely high-dimensional space. This poses a significant challenge: as dimensionality increases, the kernels can “degenerate”. In high-dimensional space, the distance between any two random points tend to converge to a constant value, making data points appears extremely sparse (i.e., <em>curse of dimensionality</em>). To address this limitation, the authors proposed training drifting models within a more compact feature space,</p> \[\mathcal{L}_\mathrm{drift}(\theta) = \mathbb{E}_{\epsilon \sim p_\epsilon}\left[ \| \phi(f_\theta(\epsilon)) - \mathrm{stopgrad} \big(\phi(f_\theta(\epsilon)) - \Delta_{p,q_\theta}( \phi(f_\theta(\epsilon))) \big) \|^2 \right] \,,\] <p>where $\phi$ denotes a pre-trained feature extractor operating on both real or generated samples. By calculating the drift in this semantic space, the model can more effectively capture high-level structures while receiving meaningful training signal.</p> <h1 id="5-conclusions">5. Conclusions</h1> <p>Drifting models introduce a novel paradigm in generative modeling by shifting the iterative refinement process—traditionally handled during inference—entirely into the training phase. By utilizing a mathematically stable drifting field, they provide a robust and efficient alternative to the adversarial instabilities of GANs and the complex requirements of diffusion model distillation.</p> <h1 id="references">References</h1> <ol class="bibliography"><li><div class="row"> <div id="deng2026generative" class="col-sm-12"> <span id="deng2026generative">Deng, M., Li, H., Li, T., Du, Y., &amp; He, K. (2026). Generative Modeling via Drifting. <i>ArXiv Preprint ArXiv:2602.04770</i>.</span> </div> </div> </li> <li><div class="row"> <div id="lai2026unified" class="col-sm-12"> <span id="lai2026unified">Lai, C.-H., Nguyen, B., Murata, N., Takida, Y., Uesaka, T., Mitsufuji, Y., Ermon, S., &amp; Tao, M. (2026). A Unified View of Drifting and Score-Based Models. <i>ArXiv Preprint ArXiv:2603.07514</i>.</span> </div> </div> </li> <li><div class="row"> <div id="yin2024one" class="col-sm-12"> <span id="yin2024one">Yin, T., Gharbi, M., Zhang, R., Shechtman, E., Durand, F., Freeman, W. T., &amp; Park, T. (2024). One-step diffusion with distribution matching distillation. <i>Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition</i>, 6613–6623.</span> </div> </div> </li> <li><div class="row"> <div id="li2015generative" class="col-sm-12"> <span id="li2015generative">Li, Y., Swersky, K., &amp; Zemel, R. (2015). Generative moment matching networks. <i>International Conference on Machine Learning</i>, 1718–1727.</span> </div> </div> </li> <li><div class="row"> <div id="unterthiner2017coulomb" class="col-sm-12"> <span id="unterthiner2017coulomb">Unterthiner, T., Nessler, B., Seward, C., Klambauer, G., Heusel, M., Ramsauer, H., &amp; Hochreiter, S. (2017). Coulomb GANs: Provably optimal Nash equilibria via potential fields. <i>ArXiv Preprint ArXiv:1708.08819</i>.</span> </div> </div> </li></ol>]]></content><author><name></name></author><category term="deep-generative-modeling"/><category term="drifting"/><summary type="html"><![CDATA[This post explores some key insights behind drifting models (Deng et al., 2026), a framework that enables high-fidelity one-step generation. By shifting the iterative process to the training phase, this approach has achieved state-of-the-art results on ImageNet.]]></summary></entry><entry><title type="html">Introduction to diffusion and flow models</title><link href="https://bacnguyencong.github.io/blog/2026/flow-diffusion-models/" rel="alternate" type="text/html" title="Introduction to diffusion and flow models"/><published>2026-02-06T15:12:00+00:00</published><updated>2026-02-06T15:12:00+00:00</updated><id>https://bacnguyencong.github.io/blog/2026/flow-diffusion-models</id><content type="html" xml:base="https://bacnguyencong.github.io/blog/2026/flow-diffusion-models/"><![CDATA[<blockquote> <p>This post covers the fundamentals of diffusion and flow matching models. The following notation and theoretical framework are primarily adapted from <a class="citation" href="#flowmodels2026">(Holderrieth &amp; Shprints, 2025)</a>.</p> </blockquote> <h1 id="1-background">1. Background</h1> <p>We begin with some mathematical definitions that help us to describe flow matching and diffusion models.</p> <p>A <strong>Trajectory</strong> is a function of form \(X\colon [0, 1] \to \mathbb{R}^d\) that maps from time $t$ to a vector in $\mathbb{R}^d$, i.e., $t \mapsto X_t$.</p> <p>A <strong>Vector field</strong> is defined as \(u\colon \mathbb{R}^d \times [0,1] \to \mathbb{R}^d\,,\) which maps from a location $x$ at time $t$ to a vector $u(x, t) \in \mathbb{R}^d$, indicating the velocity in the space, i.e., $(x, t) \mapsto u_t(x)$.</p> <p><strong>Ordinary differential equation (ODE)</strong> describes a condition on the trajectory. We want a trajectory $X_t$ to follow a line specified by the vector field $u_t(.)$. Given an initial condition, $X_0=x_0$, we define such a trajectory as the solution to the ODE equation</p> \[\begin{align*} \frac{dX_t}{dt} &amp;= u_t(X_t) \\ X_0 &amp;= x_0 \end{align*}\] <p><strong>Flow</strong> is a set of trajectories with different initial conditions. In other words, if we start at $X_0 = x_0$ and $t=0$, where we are at time $t$.</p> \[\begin{align*} \psi \colon \mathbb{R}^d \times [0, 1] &amp;\to \mathbb{R}^d \\ \psi_0(x_0) &amp;= x_0 \\ \frac{d}{dt} \psi_t(x_0) &amp;= u_t(\psi_t(x_0)) \end{align*}\] <p>For a given condition $X_0=x_0$, one can recover the trajectory via $X_t = \psi_t(x_0)$. Under the mild condition that $u$ is continuously differentiable with a bounded derivative, then the ODE has a unique solution given by a flow $\psi_t$. As a result, $\psi_t$ is a <em>diffeomorphism</em> for all $t$ (i.e., continuously differentiable with a continuously differentiable inverse $\psi_t^{-1}$).</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/ode-480.webp 480w,/assets/img/ode-800.webp 800w,/assets/img/ode-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/ode.png" class="img-fluid rounded w-75 mx-auto d-block" width="100%" height="auto" title="Relationship between ODE, vector field, and flow" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption"><b style="color: var(--global-theme-color)">Figure 1:</b> Relationship between ODE, vector field, and flow</figcaption> </figure> <p><strong>ODE solver:</strong> One of the simplest ways to solve an ODE is the <strong>Euler method</strong>. Starting with an initial condition $X_0=x_0$, we update the solution as</p> \[X_{t + h} = X_t + h \,. u_t(X_t) \,,\] <p>where $h=1/N$ is the step size.</p> <p>Another second order method is <strong>Heun’s method</strong>, which defines the update via</p> \[\begin{align*} X'_{t + h} &amp;= X_t + h \,. u_t(X_t) \\ X_{t + h} &amp;= X_t + \frac{h}{2} \,. \Big( u_t(X_t) + u_{t+h}(X'_{t + h})\Big) \end{align*}\] <p>Intuitively, it takes the first solution guess, then corrects the solution using the updated guess.</p> <h1 id="2-flow-and-diffusion-models">2. Flow and diffusion models</h1> <p>We aim to convert a base distribution $p_\mathrm{init}$ into a complex distribution $p_\mathrm{data}$. This allows us to generate samples from $p_\mathrm{data}$.</p> <h2 id="21-flow-models">2.1. Flow models</h2> <p>Consider an ODE to construct the transformation. A flow model is defined by an ODE</p> \[\begin{aligned} X_0 &amp;\sim p_\mathrm{init} \\ \frac{d}{dt} X_t &amp;= u_t^\theta (X_t) \end{aligned}\] <p>where the vector field is parameterized by a neural network $u^\theta$. The goal is to learn this vector field such that</p> \[\begin{aligned} X_1 \sim p_\mathrm{data} \Leftrightarrow \psi_1^\theta(X_0) \sim p_\mathrm{data}\,, \end{aligned}\] <p>where $\psi^\theta$ is the flow derived from vector field $u^\theta$. Once the flow model is trained, we can generate samples from $p_\mathrm{data}$ using any ODE solver.</p> <h2 id="22-diffusion-models">2.2. Diffusion models</h2> <p>Instead of using an ODE that defines deterministic trajectories, diffusion models use a stochastic differential equation (SDE) to define the transformation from $p_\mathrm{init}$ to $p_\mathrm{data}$. A stochastic trajectory is a stochastic process $X(t)$ and it is given by</p> \[\begin{align*} X_0 &amp;\sim p_\mathrm{init} \\ dX_t &amp;= u_t^\theta(X_t) dt + \sigma(t) dW \end{align*}\] <p>where $W$ is a Brownian motion, the function $\sigma\colon \mathbb{R} \to \mathbb{R}$ is called <em>diffusion coefficient</em> and the function $u_t^\theta\colon \mathbb{R}^d \times [0,1] \to \mathbb{R}^d$ is called <em>drift coefficient</em>. Every ODE is also an SDE with $\sigma(t)=0$. Note that $\sigma(t)$ is fixed and $X_t$ is a random variable for all $0 \le t \le 1$.</p> <p>A SDE can be solved using a simple method such as Euler-Maruyama, which updates</p> \[X_{t + h} = X_t + h\,. u_t^\theta (X_t) + \sqrt{h} \sigma(t) \epsilon\] <p>The above update rule looks very similar to the Euler method except the last element, where we add a Gaussian noise $\epsilon$ scaled by $\sqrt{h} \sigma(t)$.</p> <h1 id="3-flow-matching">3. Flow matching</h1> <p>As described above, both flow and diffusion models can be parameterized by a neural network $u_t^\theta(.)$ representing a vector field. We can obtain samples from this model by solving the ODE</p> \[X_0 \sim p_\mathrm{init}, \, dX_t = u_t^\theta(X_t)dt \,.\] <p>Our goal is to make $X_1 \sim p_\mathrm{data}$. So, the question is how do we train this vector field? This section will describe flow matching <a class="citation" href="#lipman2022flow">(Lipman et al., 2022; Liu et al., 2022)</a>, a technique to train $u_t^\theta$.</p> <h2 id="31-conditional-and-marginal-probability-path">3.1. Conditional and marginal probability path</h2> <p>Note that only two endpoints at $t=0$ and $t=1$ have to satisfy our conditions $p_0=p_\mathrm{init}$ and $p_1=p_\mathrm{data}$. We have some freedom to design probability distributions $p_t$ in between $0 &lt; t &lt; 1$. Changing the conditional field changes how you travel, but not where you start or end. While the distributions $p_0$ and $p_1$ don’t change, the pathway between them can change. In the following, we describe our design.</p> <p><strong>Conditional probability path</strong> is a set of distributions that gradually convert the initial distribution $p_\mathrm{init}$ into a Dirac delta distribution (i.e., a single point).</p> \[p_0(.|z) = p_\mathrm{init}, \, p_1(.|z) = \delta_z \quad \text{for all} \, z \in \mathbb{R}^d\] <p>Essentially, the conditional probability path can be considered as a trajectory in the space of distributions.</p> <p><strong>Probability path</strong> defines a set of distributions obtained by marginalizing the conditional probability path</p> \[p_t(x) = \int p_t(x|z) p_{\mathrm{data}}(z) dz \,.\] <p>It’s straightforward to see that the marginal probability path $p_t$ interpolates between $p_\mathrm{init}$ and $p_\mathrm{data}$.</p> \[p_0(.|z) = p_\mathrm{init}, \, p_1(.|z) = p_\mathrm{data}\,.\] <p>Although $p_t$ is intractable, we can sample from it as the conditional probability path is often tractable by design.</p> <blockquote class="block-tip"> <h5 id="example-gaussian-conditional-probability-path">Example: Gaussian conditional probability path</h5> <p>Let $\alpha_t, \beta_t$ denote two continuously differentiable, monotonic functions with $\alpha_0=\beta_1=0$ and $\alpha_1=\beta_0=1$. The Gaussian conditional probability path is defined as</p> \[p_t (.|z) = \mathcal{N}(\alpha_t z, \beta^2 I_d)\] <p>At the endpoints, we have</p> \[p_0(.|z) = \mathcal{N}(0, I_d) \quad \text{and} \quad p_1(.|z) = \mathcal{N}(z, 0) = \delta_z\] <p>Sampling from the marginal $p_t$ can be expressed as</p> \[z\sim p_\mathrm{data}, \epsilon \sim p_\mathrm{init} \rightarrow x = \alpha_t z + \beta_t \epsilon \,.\] </blockquote> <h2 id="32-conditional-and-marginal-vector-field">3.2. Conditional and marginal vector field</h2> <p>So far, we have designed a marginal probability path $p_t$ that the points $X_t$ along a trajectory should have. The remaining task is to find a vector field such that trajectories $X_t$ follow the probability path.</p> <p><strong>Conditional vector field</strong> is a vector field $u^\mathrm{target}_t(.\vert z)$ such that the corresponding ODE yields the conditional probability path \(p_t(.\vert z)\), i.e.,</p> \[\frac{d}{dt} X_t = u^\mathrm{target}_t(.| z) \,, X_0 \sim p_\mathrm{init} \rightarrow X_t \sim p_t(.|z)\] <p>The conditional vector field can be derived analytically by hand.</p> <blockquote class="block-tip"> <h5 id="example-conditional-gaussian-vector-field">Example: Conditional Gaussian vector field</h5> <p>Let $p(. \vert z)=\mathcal{N}(\alpha_t z, \beta_t^2 I_d)$, the conditional Gaussian vector field is given by as</p> \[u^\mathrm{target}_t (x | z) = \left( \dot{\alpha}_t - \frac{\dot{\beta}_t}{\beta_t} \alpha_t \right) z + \frac{\dot{\beta}_t}{\beta_t} x\] </blockquote> <p>Given a conditional vector field $u^\mathrm{target}_t(.\vert z)$, the <strong>marginal vector field</strong> $u^\mathrm{target}_t(x)$ can be expressed as</p> \[\begin{equation} u^\mathrm{target}_t(x) = \int u^\mathrm{target}_t(x \vert z) \frac{p_t(x|z) p_\mathrm{data}(z)}{p_t (x)} dz \label{eq:marginal_vector_field} \end{equation}\] <p>The marginal vector field follows the marginal probability path</p> \[X_0 \sim p_\mathrm{init}, \, \frac{d}{dt} X_t = u^\mathrm{target}_t(X_t) \rightarrow X_t \sim p_t(.) \quad (0 \le t \le 1)\] <h2 id="33-how-to-train-the-marginal-vector-field">3.3. How to train the marginal vector field?</h2> <p>Equation $\eqref{eq:marginal_vector_field}$ provides an intuitive way to learn the marginal vector field $u^\mathrm{target}_t(X_t)$, e.g., using mean-squared error</p> \[\mathcal{L}_\mathrm{FM} (\theta) = \mathbb{E}_{t \sim \mathcal{U}(0,1), z\sim p_\mathrm{data}, x \sim p_t(x|z)} [ \|u^\mathrm{target}_t(x) - u^\theta_t(x) \|^2]\] <p>We first draw a random time $t\in [0,1]$, then a data point $z$ from our data set. Finally, we sample $x$ from the conditional distribution and compute $u_t^\theta$. While the formula to compute $u^\mathrm{target}_t(x)$ is known, we cannot compute it efficiently. Instead, we use the conditional flow matching to learn the vector field as</p> \[\mathcal{L}_\mathrm{CFM} (\theta) = \mathbb{E}_{t \sim \mathcal{U}(0,1), z\sim p_\mathrm{data}, x \sim p_t(x|z)} [ \|u^\mathrm{target}_t(x| z) - u^\theta_t(x) \|^2]\] <p>As the conditional vector field is tractable, one can minimize the above loss easily. Although the target vector field is not the same, the marginal flow matching loss equals the conditional flow matching up to a constant, i.e.,</p> \[\mathcal{L}_\mathrm{FM}(\theta) = \mathcal{L}_\mathrm{CFM}(\theta) + C\] <p>Therefore, optimizing the conditional flow matching loss is equivalent to minimizing the flow matching loss.</p> <blockquote class="block-tip"> <h5 id="example-flow-matching-for-gaussian-conditional-probability-path">Example: Flow matching for Gaussian conditional probability path</h5> <p>Let consider the straight-path (Optimal Transport) case $\alpha_t = t$ and $\beta_t = 1 - t$, then we have</p> \[u^\mathrm{target}_t (x | z) = z - \epsilon \,.\] <p>Therefore, the objective function becomes</p> \[\mathcal{L}_\mathrm{CFM} (\theta) = \mathbb{E}_{t \sim \mathcal{U}(0,1), z\sim p_\mathrm{data}, \epsilon \sim \mathcal{N}(0, I_d)} [ \|u^\mathrm{target}_t(tz + (1 -t)\epsilon) - (z - \epsilon) \|^2]\] </blockquote> <h1 id="4-score-matching">4. Score matching</h1> <p>Unlike flow models, diffusion models <a class="citation" href="#sohl2015deep">(Sohl-Dickstein et al., 2015; Song et al., 2020)</a> use SDEs to define the transformation from $p_\mathrm{init}$ to $p_\mathrm{data}$. This section describes diffusion models and how to train them using score matching.</p> <h2 id="41-conditional-and-marginal-score-functions">4.1. Conditional and marginal score functions</h2> <p><strong>Conditional score function</strong> is defined as $\nabla_x \log p_t(x \vert z)$ i.e., the gradient of the log-likelihood of the conditional density $p_t(x \vert z)$ with respect to $x$.</p> <p><strong>Marginal score function</strong> can be derived as</p> \[\nabla_x \log p_t(x) = \frac{\nabla_x p_t(x)}{p_t(x)} = \int \nabla_x \log p_t(x | z) \frac{p_t(x|z)p_\mathrm{data}(z)}{p_t(x)} dz \,.\] <p>The result looks very similar to the relationship between the conditional and marginal vector fields.</p> <blockquote class="block-tip"> <h5 id="example-score-function-for-gaussian-probability-path">Example: Score function for Gaussian probability path</h5> <p>For the Gaussian probability path $p_t(x \vert z) = \mathcal{N}(\alpha_t z, \beta_t^2 I_d)$, the conditional score function is defined as</p> \[\nabla_x \log p_t(x | z) = - \frac{x - \alpha_t z}{\beta_t^2}\] <p>Note that the score function for the Gaussian path is a linear combination of $x$ and $z$. As a result, the conditional (marginal) vector field can be recovered from the conditional (marginal) score function.</p> </blockquote> <p>For any diffusion coefficient $\sigma_t \ge 0$, one can construct an SDE as follows:</p> \[\begin{align} dX_t = \left [u^{\mathrm{target}}(X_t) + \frac{\sigma_t^2}{2} \nabla_x \log p_t (X_t) \right ] dt + \sigma_t dW_t \,. \label{eq:sde_diffusion} \end{align}\] <p>The marginal distribution $p_t$ will be the same as in flow models, i.e., $X_t \sim p_t$. Now the trajectories are stochastic due to the nature of the SDE’s evolution. Although Equation \eqref{eq:sde_diffusion} holds for an arbitrary choice of $\sigma_t$, in practice one must carefully choose $\sigma_t$, which must be empirically determined.</p> <blockquote class="block-tip"> <h5 id="example-sde-for-gaussian-probability-path">Example: SDE for Gaussian probability path</h5> <p>For the Gaussian probability path $p_t(x \vert z) = \mathcal{N}(\alpha_t z, \beta_t^2 I_d)$, we don’t need to train the vector field $u^\theta_t$ and score function $s^\theta_t$ separately. We can simulate the SDE as</p> \[dX_t = \left[ \left(a_t + \frac{\sigma_t^2}{2} s_t^\theta(X_t) + b_t Xt \right) \right] dt + \sigma_t dW_t\] <p>where</p> \[a_t = \left( \beta_t^2 \frac{\dot{\alpha}_t}{\alpha_t} - \dot{\beta}_t\beta_t \right)\,, b_t = \frac{\dot{\alpha}_t}{\alpha_t}\] </blockquote> <h2 id="42-how-to-train-score-function">4.2. How to train score function?</h2> <p>Similarly to the marginal vector field, we can learn the score function $s^\theta_t(x)$ using the score matching loss and denoising score matching loss</p> \[\begin{align*} \mathcal{L}_\mathrm{SM}(\theta) &amp;= \mathbb{E}_{t \sim \mathcal{U}(0,1), z\sim p_\mathrm{data}, x \sim p_t(x|z)} [ \|\nabla_x \log p_t(x) - s^\theta_t(x) \|^2] \\ \mathcal{L}_\mathrm{CSM}(\theta) &amp;= \mathbb{E}_{t \sim \mathcal{U}(0,1), z\sim p_\mathrm{data}, x \sim p_t(x|z)} [ \|\nabla_x \log p_t(x| z) - s^\theta_t(x) \|^2] \end{align*}\] <p>Although the target for score function is not the same, the score matching loss is equal to the denoising score matching loss up to a constant.</p> \[\mathcal{L}_\mathrm{SM}(\theta) = \mathcal{L}_\mathrm{DSM}(\theta) + C \,.\] <blockquote class="block-tip"> <h5 id="example-score-matching-for-gaussian-probability-path">Example: Score matching for Gaussian probability path</h5> <p>For the Gaussian probability path $p_t(x \vert z) = \mathcal{N}(\alpha_t z, \beta_t^2 I_d)$, the denoising score matching loss becomes</p> \[\mathcal{L}_\mathrm{CSM}(\theta) = \mathbb{E}_{t \sim \mathcal{U}(0,1), z\sim p_\mathrm{data}, x \sim p_t(x|z)} \left[ \left\|\frac{\epsilon}{\beta_t} + s^\theta_t(\alpha_t z + \beta_t \epsilon) \right \|^2 \right]\] <p>To avoid numerical instability for $\beta_t\approx 0$, we can drop $1/\beta_t$ in the loss and reparameterize $s^\theta$ into a noise predictor network $\epsilon^\theta$ <a class="citation" href="#ho2020denoising">(Ho et al., 2020)</a>, i.e.,</p> \[\mathcal{L}_\mathrm{DDPM}(\theta) = \mathbb{E}_{t \sim \mathcal{U}(0,1), z\sim p_\mathrm{data}, x \sim p_t(x|z)} \left[ \left\|\epsilon - \epsilon^\theta_t(\alpha_t z + \beta_t \epsilon) \right \|^2 \right]\] <p>Note that one can recover the score as $s_t^\theta(x) = -\epsilon_t^\theta(x)/\beta_t$.</p> </blockquote> <h1 id="references">References</h1> <ol class="bibliography"><li><div class="row"> <div id="flowmodels2026" class="col-sm-12"> <span id="flowmodels2026">Holderrieth, P., &amp; Shprints, R. (2025). <i>MIT Computer Science Class 6.S184: Generative AI with Stochastic Differential Equations</i>. https://diffusion.csail.mit.edu/2025/index.html</span> </div> </div> </li> <li><div class="row"> <div id="lipman2022flow" class="col-sm-12"> <span id="lipman2022flow">Lipman, Y., Chen, R. T. Q., Ben-Hamu, H., Nickel, M., &amp; Le, M. (2022). Flow matching for generative modeling. <i>ArXiv Preprint ArXiv:2210.02747</i>.</span> </div> </div> </li> <li><div class="row"> <div id="liu2022flow" class="col-sm-12"> <span id="liu2022flow">Liu, X., Gong, C., &amp; Liu, Q. (2022). Flow straight and fast: Learning to generate and transfer data with rectified flow. <i>ArXiv Preprint ArXiv:2209.03003</i>.</span> </div> </div> </li> <li><div class="row"> <div id="sohl2015deep" class="col-sm-12"> <span id="sohl2015deep">Sohl-Dickstein, J., Weiss, E., Maheswaranathan, N., &amp; Ganguli, S. (2015). Deep unsupervised learning using nonequilibrium thermodynamics. <i>International Conference on Machine Learning</i>, 2256–2265.</span> </div> </div> </li> <li><div class="row"> <div id="song2020score" class="col-sm-12"> <span id="song2020score">Song, Y., Sohl-Dickstein, J., Kingma, D. P., Kumar, A., Ermon, S., &amp; Poole, B. (2020). Score-based generative modeling through stochastic differential equations. <i>ArXiv Preprint ArXiv:2011.13456</i>.</span> </div> </div> </li> <li><div class="row"> <div id="ho2020denoising" class="col-sm-12"> <span id="ho2020denoising">Ho, J., Jain, A., &amp; Abbeel, P. (2020). Denoising diffusion probabilistic models. <i>Advances in Neural Information Processing Systems</i>, <i>33</i>, 6840–6851.</span> </div> </div> </li></ol>]]></content><author><name></name></author><category term="deep-generative-modeling"/><category term="diffusion"/><category term="flow"/><summary type="html"><![CDATA[This post covers the fundamentals of diffusion and flow matching models. The following notation and theoretical framework are primarily adapted from (Holderrieth &amp; Shprints, 2025).]]></summary></entry></feed>